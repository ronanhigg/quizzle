/* Directives for jslint */
/*global define */

define([
    'vendor/underscore',
    'vendor/backbone',
    'app'
], function (_, Backbone, App) {

    "use strict";

    return Backbone.Model.extend({

    });
    
});